import styled from 'styled-components';

export const StyledCast = styled.ul`
  list-style: none;
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  .image-cover {
    background-color: white;
    border-radius: 5px;
  }
  .image-actor {
    border-radius: 5px;
  }
`;
